const C=window.TRATAK_CONFIG,m=document.getElementById('msg');let s;if(C.SUPABASE_URL.includes('PASTE'))m.textContent='Supabase not connected yet.';else s=supabase.createClient(C.SUPABASE_URL,C.SUPABASE_ANON_KEY);const cred=()=>({email:email.value.trim(),password:password.value});login.onclick=async()=>{if(!s)return;let{error}=await s.auth.signInWithPassword(cred());if(error)m.textContent=error.message;else location='dashboard.html'};signup.onclick=async()=>{if(!s)return;let{error}=await s.auth.signUp(cred());m.textContent=error?error.message:'Account created. Check your email if confirmation is enabled.'};