const C=window.TRATAK_CONFIG;if(C.SUPABASE_URL.includes('PASTE'))location='login.html';const s=supabase.createClient(C.SUPABASE_URL,C.SUPABASE_ANON_KEY);(async()=>{let{data:{user}}=await s.auth.getUser();if(!user)return location='login.html';let{data}=await s.from('memberships').select('*').eq('user_id',user.id).maybeSingle();if(!data){status.textContent='Inactive';access.textContent='No active membership found.';return}status.textContent=data.status;if(data.status==='active'){zoom.style.display='inline-block';zoom.href=C.ZOOM_JOIN_URL;access.textContent='Your membership is active.'}})();logout.onclick=async()=>{await s.auth.signOut();location='index.html'};